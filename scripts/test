#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

echo "==> Running tests with coverage..."
uv run pytest tests/ --cov=custom_components.ha_yasno --cov-report=term-missing

echo "==> Tests completed!"
